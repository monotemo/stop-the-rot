[build]
  command = "npm install"
  publish = "src/public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  # @libsql/client/http is pure JS — no external_node_modules needed

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
  force = true
